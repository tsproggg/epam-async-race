import React from "react";

export default function WinnersTable(): React.ReactNode {
  return (
    <div id="WinnersTable">
      Winners table, Pagination to display cars (10 per page).
    </div>
  );
}
